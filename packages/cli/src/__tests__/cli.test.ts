import { describe, test, expect } from 'vitest';

describe('CLI placeholder', () => {
  test('skillchk binary is configured', () => {
    // Actual CLI tests require subprocess spawning; placeholder until e2e suite
    expect(true).toBe(true);
  });
});
