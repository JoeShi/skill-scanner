#!/usr/bin/env node
// skillchk binary entry point — populated in v1 CLI implementation phase
// Commands: scan / report / list-marketplaces / list-rules / watch / --self-test

console.error("skillchk: not yet implemented — see packages/cli/src/bin.ts");
process.exit(1);
