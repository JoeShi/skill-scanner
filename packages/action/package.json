{
  "name": "@skill-scanner/github-action",
  "version": "0.1.0",
  "description": "GitHub Action wrapper for skill-scanner — CI gate + SARIF upload to GitHub Code Scanning",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@skill-scanner/core": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.0.0"
  },
  "files": [
    "dist",
    "action.yml"
  ],
  "license": "MIT"
}
