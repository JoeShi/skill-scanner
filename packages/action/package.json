{
  "name": "@skill-scanner/github-action",
  "version": "0.1.0",
  "description": "GitHub Action wrapper for skill-scanner — CI gate + GitHub Code Scanning (SARIF) integration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@skill-scanner/core": "*"
  },
  "files": [
    "dist",
    "action.yml"
  ],
  "keywords": ["skill-scanner", "github-action", "security", "sarif"],
  "license": "MIT"
}
